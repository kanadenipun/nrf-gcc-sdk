# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure(2) do |config|

  config.vm.hostname = "nrfbox"	
  config.vm.box = "ubuntu/trusty32"
  config.vm.provider "virtualbox" do |vb|
   vb.memory = "4096"
  end
end
